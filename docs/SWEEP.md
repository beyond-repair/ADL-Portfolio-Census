# Sweep notes — 2026-09-04

Source: GitHub search `user:beyond-repair` (65 items).

## Method

1. Authenticated identity: `beyond-repair`.
2. Metadata sweep of name, language, description, visibility.
3. README inspection of the active spine (Governance, SEEM, forge-aegis, clean-room, sunder).
4. Classification under ADL-Governance lifecycle + claim 0–5.
5. Gap extraction only from declared functions vs missing wiring.

## Residual

- Not every of the 65 repos is row-complete in `INVENTORY` (private and empty placeholders collapsed into `legacy`).
- Live API re-crawl is intentionally out of scope for v0.1 (would be claim inflation without a pinned auth contract).
- Physics claims remain RESEARCH / claim 2.
